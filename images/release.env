# integration-sandbox image manifest — release (latest published)
#
# Auto-generated by scripts/resolve-latest-releases.sh on 2026-05-18T19:34:23Z.
# Each OpenCHAMI service is pinned to the most recent GitHub Release tag.
# Services without a published release fall back to :latest (and are flagged).
# Override individual services as needed (SBX_<NAME>_IMAGE=...).
#
# Refresh with:  make refresh-releases

# Infra (third-party, version-pinned)
SBX_VAULT_IMAGE=hashicorp/vault:1.21
SBX_LOCALSTACK_IMAGE=localstack/localstack:3
SBX_POSTGRES_IMAGE=postgres:16-alpine

# BMC sims
SBX_SUSHY_IMAGE=quay.io/metal3-io/sushy-tools:latest
SBX_IPMI_SIM_IMAGE=openchami/ipmi-sim:dev

# OpenCHAMI services (pinned to latest GitHub Release).
# The `# renovate:` annotation in front of each line tells Renovate
# to open a bump PR whenever the upstream image publishes a new tag;
# see .github/renovate.json. `versioning=loose` because the openchami
# repos mix `v`-prefixed semver with bare semver (e.g. fru-tracker
# `0.2.2` vs smd `v2.20.3`) and loose accepts both.

# renovate: datasource=docker versioning=loose
SBX_SMD_IMAGE=ghcr.io/openchami/smd:v2.20.4
# renovate: datasource=docker versioning=loose
SBX_TOKENSMITH_IMAGE=ghcr.io/openchami/tokensmith:v0.4.1
# renovate: datasource=docker versioning=loose
SBX_BOOT_IMAGE=ghcr.io/openchami/boot-service:v0.3.0
# renovate: datasource=docker versioning=loose
SBX_METADATA_IMAGE=ghcr.io/openchami/metadata-service:v0.2.0
# renovate: datasource=docker versioning=loose
SBX_FRU_IMAGE=ghcr.io/openchami/fru-tracker:v0.2.4
# renovate: datasource=docker versioning=loose
SBX_POWER_IMAGE=ghcr.io/openchami/pcs:pr-68
# renovate: datasource=docker versioning=loose
SBX_MAGELLAN_IMAGE=ghcr.io/openchami/magellan:v0.5.1
